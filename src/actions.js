export const choose = (worker) => ({type: 'CHOOSE', value: worker});

export const clickDate = (date) => ({type: "CLICK_DATE", value: date});

export const changeRadio = (value) => ({type: "CHANGE_RADIO", value});